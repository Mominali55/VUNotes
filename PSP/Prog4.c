//Receving input from user.
#include<stdio.h>
int main()
{
    int i;
    float per;
    printf("Enter a number & percentage");
    // To take input from keyboard and store in in a memory blocks named by the specific "Variable".
    scanf("%d %f",&i,&per);
    printf("%d\n%f",i,per);
}
